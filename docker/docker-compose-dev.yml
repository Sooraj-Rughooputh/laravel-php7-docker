version: '2.1'

services:

  myproject-web:
    container_name: myproject-web
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "12100:80"
    volumes:
      - ../application:/app
    env_file:
      - .env-dev